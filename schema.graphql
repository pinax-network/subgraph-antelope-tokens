type Balance @entity {
  id:                 ID!
  owner:              String!
  balance:            BigDecimal!

  # derived fields
  block:              Block!
  token:              Token!
}

type Token @entity {
  id:                 ID!
  code:               String!
  symcode:            String!
  sym:                String!
  precision:          BigInt!

  # derived fields
  block:              Block!
  balances:           [Balance!]! @derivedFrom(field: "token")
  supply:             Supply! @derivedFrom(field: "token")
}

type Supply @entity {
  id:                 ID!
  supply:             BigDecimal!
  maxSupply:          BigDecimal!

  # derived fields
  block:              Block!
  token:              Token!
}

type Block @entity {
  id:                 ID!
  date:               String!
  month:              String!
  year:               String!
  timestamp:          Timestamp!
  number:             BigInt!
}